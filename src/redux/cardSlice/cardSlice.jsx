import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    products:[],
}

const cardSlice = createSlice({
    name:"products",
    initialState,
    reducers:{
       
        
    }
})


export default cardSlice.reducer